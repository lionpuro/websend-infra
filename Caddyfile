{
	servers {
		trusted_proxies cloudflare {
			interval 12h
			timeout 15s
		}
		trusted_proxies_strict
		client_ip_headers Cf-Connecting-Ip X-Forwarded-For
	}
}

socket.websend.org {
	reverse_proxy app:{env.SERVER_PORT} {
		header_up X-Forwarded-For {client_ip}
	}
}
